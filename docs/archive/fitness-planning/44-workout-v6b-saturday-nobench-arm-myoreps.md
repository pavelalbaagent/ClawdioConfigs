# Workout v6b (Saturday No-Bench + Arm Myorep Supersets)

Last updated: 2026-03-05

## What changed from v6

1. Saturday optional session is explicitly no-bench.
2. Added default biceps + triceps myorep superset on Saturday:
   - `Hammer Curl (myorep)` + `Kickbacks (myorep)`
3. Added optional second biceps + triceps myorep superset:
   - `Incline DB Curl (myorep optional)` + `OH DB Triceps Extension (myorep optional)`
4. Shoulder myoreps remain accessory, not the primary myorep lane.

## Historical artifact note

1. Historical export name: `Workout_v6b_nobench_sat_arm_myoreps.xlsx`
2. Historical tab: `Proposed_v6b_armmyo`
3. Historical exports under `output/spreadsheet/` are no longer tracked as source-of-truth artifacts.
4. The current reference workbook is [Workout_plan_reference.xlsx](/Users/palba/Projects/Clawdio/fitness/reference/Workout_plan_reference.xlsx).

## Why this fits your constraints

1. Keeps bench work at 2 days/week only.
2. Adds arm myorep density without adding setup complexity.
3. Maintains lat-width and incline-chest priorities in main sessions.
4. Preserves 4-5 exercises per session.
